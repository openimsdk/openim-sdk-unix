# Private HarmonyOS implementation
utssdk/app-harmony/
**/*.har

# Local native overrides; the public plugin uses Maven and CocoaPods.
utssdk/app-android/libs/
utssdk/app-ios/Frameworks/
**/*.aar
**/*.xcframework/

# Development and release evidence never belongs to the market upload.
**/.DS_Store
**/*.log
**/*.zip
**/*.apk
**/*.ipa
